__pycache__/
*.py[cod]
.venv/
.ruff_cache/
.pytest_cache/
.serena/

# 로컬 실행 산출물 — 로봇/세션마다 달라지므로 추적하지 않는다
runtime/
logs/
log/
build/
install/

# TLS 키. WebXR 텔레옵은 실행 시 로컬 CA로 새로 발급한다
*.key
*.csr
*.pem

# 수집 데이터셋과 학습 체크포인트는 용량 때문에 제외한다
data/
checkpoints/
*.deb
