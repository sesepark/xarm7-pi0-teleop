# pi0 xArm7 GUI 제어판 사용 안내

## 1. 시작 방법

처음 한 번 앱 아이콘을 설치한다.

```bash
cd ~/pi0_sehwan
./scripts/install_robot_gui_desktop.sh
```

이후 Ubuntu 앱 목록에서 `pi0 xArm7 제어판`을 검색해 실행한다. 아이콘이 보이지 않으면
다음 명령으로도 실행할 수 있다.

```bash
cd ~/pi0_sehwan
./scripts/launch_robot_gui.sh
```

브라우저가 자동으로 열리지 않으면 `http://127.0.0.1:8765`을 연다. 이 주소는 현재 로봇
PC에서만 열리며, 외부 네트워크에는 공개되지 않는다.

제어판 창을 그냥 닫아도 된다. 마지막 제어판 창이 닫힌 뒤 최대 15초 안에 GUI 서버가
종료되고, GUI가 실행한 현재 로봇 모드도 함께 종료된다. 따라서 다음 실행은 앱 아이콘을
다시 누르면 된다.

## 2. 버튼 의미

- **환경 진단·설정 안내**: Python, GELLO USB, RealSense, xArm 네트워크와 현재 오류를
  읽기 전용으로 검사한다. 로봇을 움직이지 않는다.
- **손목/전면 카메라 테스트**: 선택한 카메라를 UI의 실시간 카메라 영역에 표시한다. 각 카드의 `프리뷰 중지`로 닫는다.
- **텔레옵 시작**: 초기 자세 복귀 뒤 텔레옵을 시작하며 손목·전면 카메라 프리뷰도 자동으로 켠다.
- **데이터 수집 준비**: 두 카메라를 녹화 프로그램이 직접 사용하도록 UI 프리뷰를 자동으로 끈 뒤, `observation.images.wrist`와 `observation.images.front`를 함께 기록한다.
- **집중 모드 열기**: 자동으로 열리지 않는다. 사용자가 버튼을 누르면 손목·전면 영상을 같은 크기로 보여 주고, 녹화 시간·성공 저장·실패 폐기와 모든 수집 조작 버튼을 한 화면에 표시한다.
- **새 세션 에피소드 시간**: 데이터 수집 시작 전에 5~300초 정수를 입력한다. 선택값은 생성되는 세션 YAML에 저장되며 해당 세션 전체에 적용된다.
- 집중 모드의 `화면 전체화면`은 선택 기능이다. 녹화 시작·성공 저장·실패 폐기·전체 종료는 화면 버튼과 집중 모드 전용 단축키로 사용할 수 있다.
- 각 카메라 카드의 `크게 보기`는 집중 모드와 별개인 순수 단일 영상 화면이다.
- 집중 모드는 기존 제어판과 같은 밝은 스타일이며 초기 자세 복귀, 관절값/Endpoint 추적 선택, 텔레옵 시작, 데이터 수집 준비를 제공한다.
- 집중 모드가 열려 있을 때 녹화는 `Space` 시작, `→` 성공 저장, `←` 실패 폐기, `ESC` 전체 종료를 지원한다. 텔레옵에서는 `Space`로 텔레옵을 중지한다. 입력 칸 편집 중에는 단축키가 동작하지 않는다.
- 에피소드 경과 시간과 진행 막대는 실제 녹화 첫 프레임부터 설정 시간까지 0→100%로 표시된다.
- **초기 자세 복귀**: 작업영역 확인 체크 뒤, 3초 경고와 함께 팔 관절만 8°/s로 복귀한다.
- **텔레옵 시작**: 실행 중인 다른 모드를 종료하고 초기 자세로 복귀한 뒤 시작한다.
- **데이터 수집 준비**: 다른 모드를 종료하고 초기 자세로 복귀한 뒤 날짜별 새 세션을 만든다.
- **선택 시연 재생(1×)**: 저장된 세션과 성공 시연 번호를 고른 뒤 원본 30fps로 관절과
  기본 그리퍼를 재생한다. 현재 관절이 첫 프레임에서 2° 이내이고, 그리퍼 시작 상태와
  오류 코드가 맞을 때만 3초 대기 뒤 시작한다.
- **C31 안전 복구**: C31인지 먼저 읽기 전용으로 판정한다. 장애물 제거와 초기 자세 복귀는
  각각 별도 버튼으로 승인해야 한다.

## 3. 데이터 수집 UI 버튼

데이터 수집 준비가 끝나면 다음 화면 버튼을 쓸 수 있다.

| 동작 | UI 버튼 |
|---|---|
| 에피소드 녹화 시작 | 녹화 시작 |
| 성공으로 저장 | 성공 저장 |
| 실패로 폐기 | 실패 폐기 |
| 전체 수집 종료 | 전체 수집 종료 |

GUI 서버가 화면 버튼 요청을 로컬 녹화 프로그램의 기존 입력 통로로 전달한다. 녹화 시작
뒤에는 2초 안전 대기가 적용된다. 성공/실패 버튼은 녹화가 실제로 시작된 뒤부터 유효하다.

## 4. 저장 성공 시연 재생

`데이터 수집` 카드 아래에서 날짜/세션과 성공 시연 번호를 고른 뒤 **선택 시연 재생
(1×)** 을 누른다. 이는 추론 결과가 아니라 그 시연에 저장된 action을 그대로 30fps로
실제 xArm에 보내는 기능이다.

- 작업영역 확인 체크와 3초 대기가 필요하다.
- 시작 관절 오차가 2°를 넘거나, 그리퍼가 첫 프레임과 다르거나, 오류/경고가 있으면
  재생하지 않는다.
- **현재 모드 중지** 또는 `Ctrl+C`는 재생을 중단하고 xArm 상태 4 및 그리퍼 정지를 요청한다.
- 0.5배속 옵션은 제공하지 않으며 원본 1배속만 재생한다.

## 5. 모드 전환 규칙

한 번에 하나만 실행된다. 텔레옵에서 데이터 수집 버튼을 누르면 다음 순서다.

```text
텔레옵 안전 종료 → 초기 자세 8°/s 복귀 → 데이터 수집 사전 검증 → 새 녹화 세션 준비
```

초기 자세로 실제 이동하기 전 GUI에서 작업영역 확인 체크를 반드시 해야 한다.

## 6. 오류 안내

GUI는 로그와 doctor 결과에서 xArm 오류 코드 `C번호`를 표시한다.

텔레옵을 시작해도 카메라 프리뷰는 자동으로 열리지 않는다. 제어판의 카메라 버튼으로
필요한 카메라만 하나씩 열어 사용한다. 두 프리뷰를 동시에 열면 로봇 PC의 화면 처리
부하로 제어판 연결이 불안정해질 수 있다.

- `C31`: 충돌/비정상 전류. GUI에서만 제공되는 안전 복구 절차를 사용할 수 있다.
- 다른 코드: 자동 해제하지 않는다. 로봇 웹 UI와 공식 xArm 오류 표로 원인을 확인한 뒤
  환경 진단을 다시 실행한다.

## 7. 종료

일반 종료는 **현재 모드 중지**를 누른 뒤 창을 닫는 것을 권장한다. 급한 경우에는 창을
닫아도 최대 15초 안에 GUI가 현재 프로젝트의 실행 프로세스를 정리한다. 단, 실제 움직임이
이미 명령된 직후에는 로봇이 완전히 멈췄는지 눈으로 확인한다.
