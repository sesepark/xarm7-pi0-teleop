# xArm7 + GELLO + pi0 시연 안내서

이 문서는 **서버 학습 과정은 제외하고**, 로봇 PC에서 xArm7, GELLO, 손목 Intel RealSense 카메라, 데이터 수집 GUI를 사용하는 방법을 설명합니다. 명령어는 Ubuntu 터미널과 웹 GUI 두 가지 방법으로 정리했습니다.

## 0. 먼저 알아둘 안전 규칙

1. 로봇 주변의 사람, 케이블, 물체를 작업영역 밖으로 치웁니다.
2. 한 번에 텔레옵·녹화·카메라 미리보기 중 하나만 실행합니다.
3. 로봇이 움직이는 동안에는 로봇과 손을 가까이 대지 않습니다.
4. 이상한 자세, 큰 소리, 충돌, 오류 코드가 보이면 즉시 **현재 모드 중지**를 누릅니다.
5. 이 프로젝트의 초기 자세는 다음 TCP 위치를 기준으로 검증되었습니다.

   ```python
   arm.set_position(x=330, y=50, z=350, roll=180, pitch=0, yaw=0, speed=100, wait=True)
   ```

6. 실제 작업 문장은 다음과 같습니다.

   > 빨간색 직사각형 물체를 집어서 가운데 검은색 선의 오른쪽으로 옮긴다.

## 1. 폴더와 환경

모든 명령은 아래 폴더에서 실행합니다.

```bash
cd ~/pi0_sehwan
```

로봇·GELLO·카메라 명령은 `uf_lerobot` 환경을 사용합니다. 새 터미널을 열었거나 컴퓨터를 재부팅했다면 먼저 다음을 실행합니다.

```bash
cd ~/pi0_sehwan
source scripts/activate_uf_lerobot.sh
```

그 다음 읽기 전용 진단을 실행합니다.

```bash
./scripts/run_doctor.sh
```

`✅`가 나오면 정상입니다. `❌`가 나오면 로봇을 움직이지 말고 화면의 FAIL 항목을 먼저 해결합니다. doctor는 상태만 읽으며 모션 명령을 보내지 않습니다.

## 2. 터미널로 사용하는 방법

### 2-1. 카메라 화면만 보기

```bash
cd ~/pi0_sehwan
./run.sh camera-preview
```

카메라 창에서 `q` 또는 `ESC`를 누르면 닫힙니다. 이 명령은 xArm과 GELLO를 움직이지 않습니다.

### 2-2. xArm을 초기 자세로 보내기

작업영역을 비운 뒤 실행합니다.

```bash
cd ~/pi0_sehwan
./run.sh init-position
```

확인 문자열을 요구하면 `start`를 입력합니다. 3초 안전 대기 후 팔 관절이 최대 8°/s로 초기 자세로 이동합니다. 그리퍼는 이 과정에서 움직이지 않습니다.

### 2-3. 단순 텔레오퍼레이션

1. GELLO USB와 xArm 전원을 확인합니다.
2. 주변을 비웁니다.
3. 실행합니다.

   ```bash
   cd ~/pi0_sehwan
   ./verify.sh teleop
   ./run.sh teleop
   ```

4. 실제 시작 질문에 `start`를 입력합니다.
5. 3초 카운트다운 뒤 GELLO를 움직입니다. 처음 3초는 느린 안전 속도이고 이후 정상 속도(현재 120°/s)로 전환됩니다.
6. 끝낼 때는 `Ctrl+C`를 누르거나 별도 터미널에서 다음을 실행합니다.

   ```bash
   cd ~/pi0_sehwan
   ./run.sh stop teleop
   ```

텔레옵과 녹화를 동시에 실행하지 마세요. 모드가 꼬였으면 먼저 `./run.sh stop teleop`을 실행하고 `./run.sh init-position`으로 복귀합니다.

### 2-4. pi0용 데이터 수집

```bash
cd ~/pi0_sehwan
./run.sh record
```

실행할 때마다 `data/날짜/session_번호/`에 새 세션이 만들어집니다. 기존 폴더를 덮어쓰지 않습니다.

1. 읽기 전용 검증이 모두 통과하는지 확인합니다.
2. 프로그램을 열라는 질문에 `start`를 입력합니다.
3. 2초 후 녹화 화면이 열립니다.
4. `Space`로 한 에피소드 녹화를 시작합니다. 최대 20초입니다.
5. 녹화가 끝나면 성공/실패를 판정합니다.

   | 의미 | 키보드 | GUI 버튼 |
   |---|---|---|
   | 에피소드 녹화 시작 | `Space` | 녹화 시작 |
   | 성공으로 저장 | `→` | 성공 저장 |
   | 실패로 폐기 | `←` | 실패 폐기 |
   | 전체 수집 종료 | `ESC` | 전체 수집 종료 |

6. 판정 직후 팔은 초기 자세로 8°/s로 자동 복귀합니다. 물체를 다시 배치한 뒤 다음 `Space`를 누릅니다.
7. 모든 작업이 끝나면 `ESC`를 누릅니다.

성공 데이터만 학습에 사용합니다. 실패를 폐기해도 영상과 관절 데이터가 정상 저장되었는지 로그에 표시됩니다.

### 2-5. C31 충돌/과전류 복구

딱딱한 물체에 부딪힌 뒤 C31이 뜨면 억지로 GELLO를 움직이지 않습니다.

```bash
cd ~/pi0_sehwan
./run.sh recover-c31
```

화면 안내에 따라 **오류 확인 → 주변 장애물 제거 후 Enter → 초기 자세 복귀 확인** 순서로 진행합니다. 이 절차는 C31 전용입니다. 다른 코드에 사용하지 않습니다.

### 2-5b. C19 그리퍼/끝단 통신 복구

그리퍼 명령 중 C19가 뜨면 (흔한 원인: 손목 커넥터 접촉 불량, 카메라 케이블 간섭, baud rate 문제):

```bash
cd ~/pi0_sehwan
./run.sh recover-c19
```

**오류 확인 → 그리퍼/카메라 케이블 점검 후 Enter → 통신 복구·검증** 순서로 진행합니다.
이 절차는 팔과 그리퍼를 전혀 움직이지 않으며(잡은 물체를 떨어뜨리지 않음), 통신만
복구합니다. 복구 직후 C19가 재발하면 케이블 교체나 비상 정지 버튼 재시작이 필요합니다.

### 2-6. 저장한 성공 시연 재생

다음은 저장된 action을 원본 30fps(1배속)로 실제 xArm과 기본 그리퍼에 보내는 명령입니다.

```bash
cd ~/pi0_sehwan
./run.sh replay data/2026-07-30/session_005 0
```

마지막 숫자 `0`은 첫 번째 성공 시연입니다. 실행 전 오류 0, 현재 관절과 첫 프레임의 최대
오차 2° 이하, 그리퍼 시작 상태를 검사합니다. 통과 후 `start`와 3초 대기를 거쳐 시작합니다.
중단은 `Ctrl+C`입니다. 0.5배속은 제공하지 않습니다.

## 3. GUI로 사용하는 방법

### 3-1. 최초 한 번만 아이콘 설치

```bash
cd ~/pi0_sehwan
./scripts/install_robot_gui_desktop.sh
```

Ubuntu 앱 목록에서 `pi0 xArm7 제어판`을 검색해 실행합니다. 아이콘이 안 보이면 다음 명령으로 실행합니다.

```bash
cd ~/pi0_sehwan
./scripts/launch_robot_gui.sh
```

브라우저가 자동으로 열리지 않으면 `http://127.0.0.1:8765`를 엽니다. 이 주소는 로봇 PC 안에서만 열립니다.
창을 닫으면 최대 15초 안에 GUI 서버와 GUI가 시작한 현재 모드가 함께 정리됩니다. 다음에는
앱 아이콘을 다시 누르면 됩니다.

### 3-2. GUI 버튼 순서

- **환경 진단**: doctor를 실행합니다. 읽기 전용입니다.
- **카메라 보기**: 손목 카메라 창을 엽니다.
- **초기 자세 복귀**: 안전 확인 후 3초 대기와 함께 초기 자세로 이동합니다.
- **텔레옵 시작**: 실행 중 모드를 종료하고 초기 자세로 복귀한 뒤 텔레옵을 시작합니다.
- **데이터 수집 준비**: 실행 중 모드를 종료하고 초기 자세로 복귀한 뒤 새 날짜별 세션을 준비합니다.
- **현재 모드 중지**: 텔레옵·녹화를 중지합니다.
- **C31 복구**: C31인지 먼저 확인하고, 장애물 제거와 복귀를 각각 별도 버튼으로 승인합니다.

GUI는 기존 터미널의 로그를 화면 아래에 보여줍니다. `start`, `Enter`, `return` 같은 입력은 버튼으로 대신할 수 있습니다. 녹화의 `Space`, `→`, `←`, `ESC`는 GUI 버튼과 무선 키보드 모두 사용할 수 있습니다.

버튼을 눌러 모드를 바꿀 때 GUI가 자동으로 다음 순서를 지킵니다.

```text
기존 모드 중지 → 초기 자세 복귀 → 3초 안전 대기 → 새 모드 시작
```

### 3-3. GUI에서 오류가 보일 때

오류 창에는 `C번호`, 의미, 즉시 할 일이 표시됩니다. **C31만** 준비된 복구 버튼을 사용하고, 그 밖의 오류는 원인을 제거한 뒤 doctor를 다시 실행합니다.

## 4. 오류 코드 빠른 표

아래 표는 xArm 컨트롤러 오류의 초보자용 요약입니다. 같은 번호라도 로그에 함께 표시되는 세부 코드가 있으면 그 값을 같이 기록해 담당자에게 전달합니다.

| 코드 | 의미 | 첫 조치 |
|---|---|---|
| C1 | 비상 정지 버튼 | 원인 확인·버튼 물리 해제 후 C1 복구 절차 (`./run.sh recover-c1` 또는 GUI 버튼) |
| C2/C3 | 외부/3상 비상 입력 | 안전 스위치와 IO를 확인 |
| C10 | 서보 모터 오류 | 움직이지 말고 상세 코드 확인 |
| C11~C17 | J1~J7 서보 오류 | 해당 관절의 걸림·케이블·과열 확인 |
| C18/C28 | 센서·끝단 모듈 통신 | 손목/그리퍼 케이블과 전원 확인 |
| C19 | 그리퍼/끝단 통신 오류 | 케이블 점검 후 C19 복구 절차 (`./run.sh recover-c19` 또는 GUI 버튼) |
| C21 | 운동학 계산 오류 | 목표 자세를 작업영역 안으로 변경 |
| C22 | 자기 충돌 | 즉시 중지하고 링크 간섭 확인 |
| C23 | 관절 각도 제한 | 제한을 넘는 목표 취소 |
| C24/C30 | 속도 또는 피드백 속도 오류 | 속도를 낮추고 급격한 리더 동작 금지 |
| C25/C32/C40 | 경로·원호·IK 계산 실패 | 더 단순하고 가까운 목표로 재시도 |
| C26/C27 | 실시간 처리·명령 응답 오류 | 모드 종료, 네트워크/컨트롤러 확인 |
| C31 | 충돌 또는 비정상 전류 | 장애물 제거 후 C31 복구 절차 |
| C33 | GPIO 오류 | 외부 IO와 배선 확인 |
| C34/C36 | 기록 시간·지연 명령 한도 | 모드 종료 후 명령 빈도와 저장 상태 확인 |
| C35 | 안전 경계 초과 | 작업영역과 사람 위치 확인 |
| C37/C38 | 비정상 수동 움직임·관절 각도 | 팔을 놓고 걸림/엔코더 점검 |
| C39/C110/C111 | 전원·보드 통신 오류 | 전원/케이블 확인, 반복 시 담당자 보고 |

### C31이 특히 자주 나는 이유

C31은 실제 관절 전류가 예상값보다 커졌다는 뜻입니다. 단단한 물체와의 충돌 외에도 손목 카메라 케이블 걸림, 그리퍼나 카메라의 무게 설정(TCP payload) 오류, 너무 빠른 움직임, 마찰 설정 문제가 원인일 수 있습니다. 먼저 물체와 케이블을 치우고, 반복되면 payload·속도·충돌 감도 설정을 점검합니다.

## 5. 공식 참고 자료

- [UFACTORY 공식 C31 해결 안내](https://docs.supportarticle.ufactory.cc/support_articles/software/how-to-solve-c31-error.html)
- [xArm Python SDK 공식 API 오류 코드 표](https://github.com/xArm-Developer/xArm-Python-SDK/blob/master/doc/api/xarm_api_code.md)

이 GUI와 문서는 오류를 **자동으로 무시하기 위한 도구가 아닙니다**. 오류가 반복되거나 원인을 모르면 모드를 중지하고 로그 파일과 오류 번호를 담당자에게 전달하세요.
