# 1단계: 재부팅 후에도 같은 환경으로 시작하기

이 문서는 xArm7, GELLO, RealSense D435i, pi0를 서로 섞지 않고 안전하게 준비하는 방법을 설명합니다.

## 먼저 이해할 점

이 프로젝트에는 역할이 다른 Python 환경 두 개가 필요합니다.

| 환경 | 하는 일 | Python/LeRobot |
|---|---|---|
| `uf_lerobot` | xArm 제어, GELLO, 카메라, 데이터 수집 | Python 3.10, LeRobot 0.4.3 |
| `openpi/.venv` | pi0 학습과 policy server | Python 3.11, 현재 openpi 의존성 |

두 환경을 합치지 않는 이유는 LeRobot 버전이 서로 달라 충돌하기 때문입니다.

ROS는 RViz/MoveIt용 보조 환경입니다. 이번 데이터 수집과 pi0 제어의 주 환경은 아닙니다.

## GELLO "로그인"의 의미

GELLO에 로그인하는 과정은 없습니다. Linux는 USB serial 장치를 `dialout` 그룹 사용자에게만 열어 줍니다.

다음 명령은 **사용자가 직접** 실행해야 합니다. sudo 암호 입력이 필요합니다.

```bash
sudo usermod -aG dialout $USER
```

그 다음 Ubuntu에서 **로그아웃 후 다시 로그인**하거나 컴퓨터를 재부팅합니다. 새 터미널만 열어서는 권한이 적용되지 않습니다.

로그인 후 다음 결과에 `dialout`이 보이면 성공입니다.

```bash
groups
```

## 매번 시작하는 방법

### A. 로봇/GELLO/카메라 터미널

```bash
cd /home/robotics/pi0_sehwan
source scripts/activate_uf_lerobot.sh
python scripts/doctor.py
```

doctor는 로봇을 움직이지 않습니다. Python 버전, 패키지, GELLO USB 권한, 카메라, GPU, xArm 네트워크와 현재 오류 상태만 읽습니다.

처음 설치가 끝난 현재 환경은 Python 3.10, LeRobot 0.4.3, xArm SDK 1.18.4,
GELLO/Dynamixel, pyrealsense2를 사용합니다. `doctor`가 이 구성 요소가 빠졌는지도 확인합니다.

## 텔레옵은 두 명령만 사용합니다

```bash
# 준비 상태만 확인합니다. 로봇을 절대 움직이지 않습니다.
./verify.sh teleop

# GELLO encoder 값만 읽습니다. xArm에는 연결하지 않습니다.
./verify.sh gello

# 검증을 통과한 경우에만 실제 GELLO 텔레옵을 시작합니다.
./run.sh teleop
```

`run.sh teleop`은 이전에 이 프로젝트가 시작한 텔레옵만 정리하고, 전용 Python 환경에서
실행합니다. 다른 ROS/연구실 프로세스를 무차별 종료하지 않습니다. 실행 로그는 `logs/teleop/`에 저장됩니다.

현재는 GELLO 관절 방향을 아직 검증하지 않았으므로 `./verify.sh teleop`이 의도적으로 실패합니다.
이는 안전장치입니다. 2단계에서 J1~J7 및 그리퍼의 방향을 확인한 뒤에만 실제 실행이 허용됩니다.

## 2단계: GELLO 관절 방향 확인

먼저 GELLO 값만 읽으려면 다음을 실행합니다.

```bash
./verify.sh gello
```

실제 방향 시험은 한 관절씩만 실행합니다. 아래 예시는 J1에 `+1` 부호를 후보로 적용해
최대 1°만 움직여 보는 명령입니다.

```bash
./run.sh gello-axis 1 1
```

GELLO의 물리 관절 위치나 Dynamixel ID가 헷갈리면 먼저 아래 명령을 사용합니다. xArm에는
모션 명령을 보내지 않고, 사용자가 편하게 움직인 GELLO 관절이 어떤 Dynamixel ID인지 출력합니다.

```bash
./run.sh gello-detect
```

방향 시험에서는 GELLO 관절을 정확히 1° 움직일 필요가 없습니다. 선택한 물리 관절을
3°~30° 정도 편하게 움직인 뒤 안내에 따라 Enter를 누릅니다. 프로그램은 encoder로 실제
변화를 확인하고, xArm에는 언제나 1°만 명령합니다. 화면에 표시된 xArm의 예상 변화량을
확인하고, 다시 `test`를 입력해야만 실제 모션이 발생합니다. 시험 후에는 Enter를 누르면
기록된 안전 시작 자세로 돌아갑니다.

GELLO와 xArm이 물리적으로 같은 방향으로 움직이면 해당 부호를 유지하고, 반대면 후보 부호를
`-1`로 바꿔 다시 시험합니다. 예: `./run.sh gello-axis 1 -1`

### B. pi0 학습 또는 policy server 터미널

```bash
cd /home/robotics/pi0_sehwan/openpi
source .venv/bin/activate
```

### C. ROS/RViz가 필요한 경우에만 쓰는 터미널

```bash
source /opt/ros/humble/setup.bash
source /home/robotics/dev_ws/install/setup.bash
```

`pi0_sehwan` 루트에서는 `colcon build`를 실행하지 않습니다. ROS workspace는 `/home/robotics/dev_ws`입니다.

## test.py는 무엇인가

`python test.py`는 현재 xArm의 상태만 읽고 절대 움직이지 않습니다.

실제 모션은 아직 하지 않습니다. 나중에 안전 자세를 사용자와 합의해 `config/safe_motion.yaml`을 만든 다음에만 아래처럼 실행할 수 있습니다.

```bash
python test.py --execute --config config/safe_motion.yaml --target 안전자세이름
```

실행에는 옵션, 설정 파일, 목표 이름 재입력의 세 가지 확인이 모두 필요합니다.
